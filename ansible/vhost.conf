<VirtualHost {{ domain }}:80>
    ServerAdmin {{ email }}
    ServerAlias {{ domain }}
    DocumentRoot {{ docroot }}/jekyll/_site/
</VirtualHost>

UseCanonicalName    Off
VirtualDocumentRoot {{ docroot }}/subdomains/%1/webroot

